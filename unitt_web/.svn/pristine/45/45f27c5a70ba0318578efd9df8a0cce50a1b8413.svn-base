package command.category;
/*package command.category;

import java.util.ArrayList;

import javax.servlet.http.HttpServletRequest;
import javax.servlet.http.HttpServletResponse;

import bean.CategoryBean;
import command.CommandHandler;
import dao.CategoryDao;

public class CategoryService implements CommandHandler {
	@Override
	public String process(HttpServletRequest request, HttpServletResponse response) throws Throwable {
		String countNum ="";
		request.setCharacterEncoding("UTF-8");
		String category = request.getParameter("select"); // ±³À°
		CategoryDao dao = new CategoryDao();
		ArrayList<CategoryBean> returnResult = dao.searchDao(category);
		request.setAttribute("title", category);
		request.setAttribute("categoryData", returnResult);
		countNum =String.valueOf(returnResult.size()-1);
		request.setAttribute("checkCount", countNum); // ¹Ýº¹È½¼ö ÁöÁ¤

		return "/view/categoryView/categoryResult.jsp";
	}
}*/
